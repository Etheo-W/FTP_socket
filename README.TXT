# FTP_socket

分别通过两个终端进入server/client文件夹，直接执行命令./build.sh就可编译连接执行；

注意：因为最终的可执行程序名为socketServer/socketClient，
     所以最终生成可执行程序的路径下不可包含socketServer/socketClient名字的文件及文件夹，
     否则会提示“ld: can't open output file for writing: socketServer, errno=21 for architect”

